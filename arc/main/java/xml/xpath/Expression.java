package xml.xpath;

/**
 * An expression is a logical expression that evaluates into a sequence 
 * containing zero or more items.
 * The xpath language provides several kinds of expressions which may be 
 * constructed from keywords, symbols and operands. In general, the operands of 
 * an expression are other expressions.
 */
public interface Expression {

}
